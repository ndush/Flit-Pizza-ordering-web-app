# Leave MONGODB_URI empty in development to use a zero-setup embedded MongoDB (data kept in .data/).
MONGODB_URI=
# Required in production. Any long random string.
JWT_SECRET=
# Admin account created on boot when none exists (dev defaults: admin@flit.test / admin123).
ADMIN_EMAIL=admin@flit.test
ADMIN_PASSWORD=
